# Repository instructions

- Maintain `CHANGELOG.md` in Keep a Changelog format.
- Add user-visible changes under `Unreleased` in the same commit as the change.
- When releasing, move those entries into a versioned section dated `YYYY-MM-DD`
  and update the comparison links.
